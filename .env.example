# Base URL of the Bumi Watch Nemotron backend (bumi-watch-nemotron).
# Local: `npm start` in the backend runs it on port 3001.
# For a Firebase deploy, set this to the deployed backend URL before `npm run build`.
VITE_API_URL="http://localhost:3001"
